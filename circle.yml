dependencies:
  cache_directories:
    - "~/nilearn_data"
  override:
    - pip install --upgrade pip
    - pip install -e .
    - pip install sphinx matplotlib coverage Pillow

test:
  override:
    - make clean test
    - cd doc && make html