dependencies:
  override:
    - pip install --upgrade pip
    - pip install --upgrade numpy
    - pip install --upgrade scipy scikit-learn nibabel sphinx

test:
  override:
    - make
    - cd doc && make